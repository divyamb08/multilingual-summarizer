// Import the existing Promise.withResolvers polyfill
import { setupPromiseWithResolversPolyfill } from './utils/promisePolyfill';

// Setup the polyfill if needed
setupPromiseWithResolversPolyfill();
